const defaultPosition = ""
const defaultPositionAsBoard = fenToBoardArray(defaultPosition)

function boardArrayToFen(board) {
  let fen = ""
  return fen
}

function fenToBoardArray(fen) {
  let board = []
  return board
}

function isLegalMove(move) {
  // Move must be on the board
  return true
}

function getLegalMoves(piece) {
  return []
}
